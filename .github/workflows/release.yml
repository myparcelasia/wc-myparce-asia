name: Release Plugin

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Extract Version
        id: version
        run: |
          VERSION=$(grep -E '^\s*\*\s*Version:' myparcel-asia.php | awk '{print $3}' | tr -d '\r')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Extracted version: $VERSION"

      - name: Extract Changelog
        id: changelog
        run: |
          # Extract the changelog block for the latest version
          CHANGELOG=$(awk -v ver="[${{ env.VERSION }}]" '
            $0 ~ "^## " ver {flag=1; next}
            /^## \[/ {if (flag) exit}
            flag {print}
          ' CHANGELOG.md | sed '/^[[:space:]]*$/d')
          
          # Convert markdown list to HTML ul/li for the JSON output
          HTML_CHANGELOG="<h4>${{ env.VERSION }}</h4><ul>"
          while read -r line; do
            if [[ $line == -* ]]; then
              item=$(echo "$line" | sed 's/^- //')
              HTML_CHANGELOG="${HTML_CHANGELOG}<li>${item}</li>"
            elif [[ -n $line ]]; then
              HTML_CHANGELOG="${HTML_CHANGELOG}<strong>${line//### /}</strong><br>"
            fi
          done <<< "$CHANGELOG"
          HTML_CHANGELOG="${HTML_CHANGELOG}</ul>"
          
          echo "CHANGELOG<<EOF" >> $GITHUB_ENV
          echo "$HTML_CHANGELOG" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Generate JSON
        run: |
          cat << 'EOF' > wc-myparcel-asia.json
          {
              "name": "MyParcel Asia",
              "slug": "myparcel-asia",
              "version": "${{ env.VERSION }}",
              "author": "<a href='https://myparcelasia.com'>MyParcel Asia</a>",
              "download_url": "https://github.com/myparcelasia/wc-myparce-asia/raw/refs/heads/main/release/myparcel-asia-${{ env.VERSION }}.zip",
              "requires": "5.0",
              "tested": "6.5",
              "sections": {
                  "description": "Shipping and fulfilment plugin by MyParcel Asia.",
                  "changelog": ""
              }
          }
          EOF
          jq --arg changelog "${{ env.CHANGELOG }}" '.sections.changelog = $changelog' wc-myparcel-asia.json > tmp.json && mv tmp.json wc-myparcel-asia.json

      - name: Create Zip Archive
        run: |
          mkdir -p build/myparcel-asia
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' --exclude='release' --exclude='wc-myparcel-asia.json' --exclude='*.zip' ./ build/myparcel-asia/
          cd build
          zip -r ../myparcel-asia-${{ env.VERSION }}.zip myparcel-asia/

      - name: Commit to Release Directory
        run: |
          mkdir -p release
          # Clean previous releases in the directory
          rm -f release/*.zip release/*.json
          mv wc-myparcel-asia.json release/
          mv myparcel-asia-${{ env.VERSION }}.zip release/
          
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -f release/
          git commit -m "chore: update release files [skip ci]" || echo "No changes to commit"
          git push
